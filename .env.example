# Copy this file to .env and update values
# Set USE_SUPABASE to true to enable Supabase integration
USE_SUPABASE=true
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-service-role-or-anon-key

# Optional local DB values (if you're not using Supabase)
DB_HOST=localhost
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=litc_portal

# Notes:
# - For server-side operations (insert/update/delete) use the Supabase service role key (keep it secret).
# - For client-side auth (OAuth redirect) you'll configure Supabase Auth providers (e.g., GitHub) in the Supabase dashboard.
